torch>=2.0.0
torchdiffeq>=0.2.3
torch-geometric>=2.4.0
networkx>=3.0
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
tqdm>=4.65
matplotlib>=3.7
PyYAML>=6.0
aiohttp>=3.9
requests>=2.31

# optional: local Qwen model for src/update_movie_list.py --clean-titles
# Qwen3.5 is a very recent release - if `pip install transformers` doesn't
# recognize the model architecture, upgrade to the latest transformers
# release (or install from source) rather than assuming this floor is enough.
transformers>=4.51.0
accelerate>=0.30.0

# optional community-detection backends used by advance_kg.py / graph_text.py
python-louvain>=0.16
# umap-learn>=0.5        # optional: better embedding dim-reduction, falls back to PCA
# leidenalg>=0.10        # optional: Leiden community detection, falls back to Louvain
# python-igraph>=0.11    # required by leidenalg
# infomap>=2.7           # optional: Infomap community detection, falls back to Louvain
